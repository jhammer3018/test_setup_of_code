from setuptools import setup

setup(
   name='test_function',
   version='1.0',
   description='A test for if i can write a setup file',
   license = "MIT", 
   author='JH',
   author_email='jh4189@columbia.edu',
   packages=['test_function']  #same as name exclude external packages as dependencies
)
